<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.natanhp.football_league">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.FavoriteTeamListActivity" />
        <activity android:name=".view.SearchTeamActivity" />
        <activity android:name=".view.DetailTeamActivity" />
        <activity android:name=".view.TeamListActivity" />
        <activity android:name=".view.StandingsActivity" />
        <activity android:name=".view.FavoriteMatchActivity" />
        <activity android:name=".view.MatchDetailActivity" />
        <activity android:name=".view.SearchMatchActivity" />
        <activity
            android:name=".view.MatchActivity"
            android:label="@string/title_activity_match"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.DetailLeagueActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>